// Q11 storing the names of a few of my friends in an array called names and
// printing each person name by accessing the each element in the list one at a time.
let names: string[] = ["Ahmed", "Ahsan", "Farhan"];
for (let i = 0; i < names.length; i++) {
    console.log(names[i]);
}